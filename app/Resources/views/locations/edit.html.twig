{% extends 'base.html.twig' %}
{% block body %}
    <h1>{%trans%}location.Edition{%endtrans%} '{{location.name}}'</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}