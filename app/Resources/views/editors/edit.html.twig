{% extends 'base.html.twig' %}
{% block body %}
    <h1>{%trans%}editor.Edition{%endtrans%} '{{editor.name}}'</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}