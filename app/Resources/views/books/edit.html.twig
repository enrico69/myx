{% extends 'base.html.twig' %}
{% block body %}
    <h1>{%trans%}book.Edition{%endtrans%}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}{# empty Twig template #}
