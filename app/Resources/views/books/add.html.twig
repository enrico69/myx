{% extends 'base.html.twig' %}
{% block body %}
    <h1>{%trans%}book.Addition{%endtrans%}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <script type="text/javascript">
        $(document).ready(function() {
            resizeCheckBoxes();
        });
    </script>
{% endblock %}